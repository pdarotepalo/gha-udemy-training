name: "Lecture 1: Output Information"

on: workflow_dispatch

jobs:
  info:
    runs-on: ubuntu-latest
    steps:
      - name: Output GitHub Context
        run: |
          echo "GitHub Context JSON:"
          echo "${{ toJSON(github) }}"
