name: First Workflow

on: workflow_dispatch

jobs:
  job-name-goes-here:
    runs-on: ubuntu-latest
    steps:
      - name: Print greeting
        run: echo "Hello world!"
      - name: Print goodbye
        run: echo "Goodbye!"
