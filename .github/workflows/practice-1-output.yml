name: "Practice 1: 🔔 Issues Notification"

on:
  issues:
    types: [opened, edited, deleted]

jobs:
  notigication:
    name: 🔔 Notify
    runs-on: ubuntu-latest
    steps:
      - name: 🚨 Alert
        run: echo "The issue number ${{ github.event.issue.number }} has been ${{ github.event.action }}."
