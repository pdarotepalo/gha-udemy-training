name: "Practice 1: 🔔 Issues Notification"

on:
  issues:
    types: [opened, edited, deleted, closed, reopened]

jobs:
  notigication:
    name: 🔔 Notify
    runs-on: ubuntu-latest
    steps:
      - name: 🚨 Alert
        run: echo "The issue ${{ github.event.issue.html_url }} has been ${{ github.event.action }}."
