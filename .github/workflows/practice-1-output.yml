name: 🔔 Issues Notification

on:
  issues:
    types: [opened, edited, deleted]

jobs:
  notigication:
    name: 🔔 Notify
    runs-on: ubuntu-latest
    steps:
      - name: 🚨 Alert
        run: echo "An issue has been ${{ github.event_name }}"
